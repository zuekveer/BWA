#teapot example. Do it in prod. (no)

# Database URI
DB_URI=postgresql://${PG_USER}:${PG_PASSWORD}@${DB_HOST}:${DB_PORT}/${PG_DATABASE}

# HTTP Server configuration
HTTP_PORT=8088                    # Port for the Go application (matches APP_PORT from previous .env)

# Logger configuration
LOG_LEVEL=info                    # Log level for the application (can be set to debug, warn, error, etc.)

# Existing variables for use in DB_URI interpolation
PG_DATABASE=pgdatabase            # Database name
PG_USER=pguser                    # Database username
PG_PASSWORD=pgpassword            # Database password
DB_HOST=db                        # Database host as per Docker Compose service name
DB_PORT=5432                      # PostgreSQL port within the container network
